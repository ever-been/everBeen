class ET1Comparator
!!!413698.java!!!	compare(inout o1 : FileEntry, inout o2 : FileEntry) : int
			
			if (o1.getTimeStamp() > o2.getTimeStamp()) {
				return 1;
			} else if (o1.getTimeStamp() < o2.getTimeStamp()) {
				return -1;
			} else {
				return 0;
			}
