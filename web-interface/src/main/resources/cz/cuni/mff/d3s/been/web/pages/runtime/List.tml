<!DOCTYPE html>

<html t:type="layout" section="section"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd" xmlns:p="tapestry:parameter">

    <div class="container">
        <div class="row">
            <div class="span12">
                <t:grid t:source="runtimes" t:row="runtime" t:inPlace="true"
                        t:exclude="workingDirectory,tasksWorkingDirectory,setTaskDirs,setTasksWorkingDirectory,setStartTime,exclusiveId,port"
                        t:add="load,memory, other, startTime" t:class="table table-hover"
                        t:rowClass="getRowClass(runtime)">
                    <p:idCell>
                        <t:pagelink t:page="runtime/detail" t:context="runtime.id" data-placement="top"
                                    data-toggle="tooltip"
                                    data-original-title="${runtime.id}" class="show_tooltip">
                            ${shorten(runtime.id, 5, 5)}
                        </t:pagelink>
                    </p:idCell>
                    <p:loadCell>
                        ${runtime.monitorSample.loadAverage.load1}
                    </p:loadCell>
                    <p:hostCell>
                        <span data-placement="top" data-toggle="tooltip" class="show_tooltip"
                              data-original-title="${runtime.host}:${runtime.port}">${shorten(runtime.host, 10, 10)}:${runtime.port}
                        </span>
                    </p:hostCell>
                    <p:memoryCell>
                        ${getMemoryInMB(runtime)} MB
                    </p:memoryCell>
                    <p:startTimeCell>
                        ${getStartTime(runtime)}
                    </p:startTimeCell>
                    <p:otherCell>
                        <t:if t:test="getInfo(runtime)">
                            <i style="font-size: 2em; cursor: help; color:red" data-placement="top"
                               data-toggle="tooltip" class="icon-info-sign icon-white show_tooltip"
                               data-original-title="${getInfo(runtime)}"></i>
                        </t:if>
                    </p:otherCell>
                </t:grid>
            </div>
        </div>
    </div>


</html>