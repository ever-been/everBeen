class PackageType
!!!613762.java!!!	toString() : String
		return STRINGS[ordinal()];
!!!613890.java!!!	realValueOf(in name : String) : PackageType
    if (name == null) {
      throw new NullPointerException("Name is null");
    }
		for (int i = 0; i < VALUES.length; i++) {
			if (name.equals(STRINGS[i])) {
				return VALUES[i];
			}
		}
		throw new IllegalArgumentException("No enum value named \"" + name
			+ "\" exists.");
!!!614018.java!!!	getSuffix() : String
		return SUFFIXES[ordinal()];
