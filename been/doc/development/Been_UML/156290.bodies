class IDManager
!!!265730.java!!!	getInstance() : IDManager
		return MGR;
!!!265858.java!!!	getNext(inout clazz : Class<T>) : T
		Counter ctr = idSet.get(clazz);
		if (ctr == null) {
			ctr = new Counter();
			idSet.put(clazz, ctr);
		}
		
		try {
			T instance = clazz.newInstance();
			instance.setValue(ctr.getNext());
			return instance;
		} catch (Exception e) {
			e.printStackTrace();
		}
		
		assert false : "Could not create new instance for: " + clazz.getName();
		return null;
