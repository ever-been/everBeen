diff -ur ../before/omni/src/appl/utils/catior/catior.cc omni/src/appl/utils/catior/catior.cc
--- ../before/omni/src/appl/utils/catior/catior.cc	2005-05-22 14:39:44.000000000 +0200
+++ omni/src/appl/utils/catior/catior.cc	2009-05-28 11:30:13.000000000 +0200
@@ -88,15 +88,15 @@
 int
 getopt(int num_args, char* const* args, const char* optstring)
 {
-  if (optind == num_args) return EOF;
+  if (optind == num_args) return -1;
   char* buf_left = *(args+optind);
 
-  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return EOF;
+  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return -1;
   else if ((optind < (num_args-1)) && strcmp(buf_left,"-") == 0 &&
 	   strcmp(*(args+optind+1),"-") == 0)
     {
       optind+=2;
-      return EOF;
+      return -1;
     }
   else if (strcmp(buf_left,"-") == 0)
     {
@@ -360,7 +360,7 @@
   int hexflag = 0;
   int omniflag = 0;
 
-  while((c = getopt(argc,argv,"xo")) != EOF) {
+  while((c = getopt(argc,argv,"xo")) != -1) {
     switch(c) {
     case 'x':
       hexflag = 1;
diff -ur ../before/omni/src/appl/utils/convertior/convertior.cc omni/src/appl/utils/convertior/convertior.cc
--- ../before/omni/src/appl/utils/convertior/convertior.cc	2005-05-22 14:39:43.000000000 +0200
+++ omni/src/appl/utils/convertior/convertior.cc	2009-05-28 11:30:13.000000000 +0200
@@ -85,15 +85,15 @@
 int
 getopt(int num_args, char* const* args, const char* optstring)
 {
-  if (optind == num_args) return EOF;
+  if (optind == num_args) return -1;
   char* buf_left = *(args+optind);
 
-  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return EOF;
+  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return -1;
   else if ((optind < (num_args-1)) && strcmp(buf_left,"-") == 0 && 
 	   strcmp(*(args+optind+1),"-") == 0)
     {
       optind+=2;
-      return EOF;
+      return -1;
     }
   else if (strcmp(buf_left,"-") == 0)
     {
@@ -150,7 +150,7 @@
 
   int c;
 
-  while((c = getopt(argc,argv,"x")) != EOF)
+  while((c = getopt(argc,argv,"x")) != -1)
     {
       switch(c)
 	{
diff -ur ../before/omni/src/appl/utils/genior/genior.cc omni/src/appl/utils/genior/genior.cc
--- ../before/omni/src/appl/utils/genior/genior.cc	2005-01-07 00:08:53.000000000 +0100
+++ omni/src/appl/utils/genior/genior.cc	2009-05-28 11:30:13.000000000 +0200
@@ -79,15 +79,15 @@
 int
 getopt(int num_args, char* const* args, const char* optstring)
 {
-  if (optind == num_args) return EOF;
+  if (optind == num_args) return -1;
   char* buf_left = *(args+optind);
 
-  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return EOF;
+  if ((*buf_left != '-' && *buf_left != '/') || buf_left == NULL ) return -1;
   else if ((optind < (num_args-1)) && strcmp(buf_left,"-") == 0 && 
 	   strcmp(*(args+optind+1),"-") == 0)
     {
       optind+=2;
-      return EOF;
+      return -1;
     }
   else if (strcmp(buf_left,"-") == 0)
     {
@@ -142,7 +142,7 @@
   int c;
   int hexflag = 0;
 
-  while((c = getopt(argc,argv,"x")) != EOF) {
+  while((c = getopt(argc,argv,"x")) != -1) {
     switch(c) {
     case 'x':
       hexflag = 1;
diff -ur ../before/omni/src/tool/omkdepend/def.h omni/src/tool/omkdepend/def.h
--- ../before/omni/src/tool/omkdepend/def.h	2008-12-12 13:13:19.000000000 +0100
+++ omni/src/tool/omkdepend/def.h	2009-05-28 11:31:24.000000000 +0200
@@ -129,7 +129,7 @@
 
 char			*copy();
 char			*base_name();
-char			*getline();
+char			*get_line();
 struct symtab		*slookup();
 struct symtab		*isdefined();
 struct symtab		*fdefined();
diff -ur ../before/omni/src/tool/omkdepend/main.c omni/src/tool/omkdepend/main.c
--- ../before/omni/src/tool/omkdepend/main.c	2008-12-29 15:38:31.000000000 +0100
+++ omni/src/tool/omkdepend/main.c	2009-05-28 11:31:24.000000000 +0200
@@ -470,7 +470,7 @@
  * Get the next line.  We only return lines beginning with '#' since that
  * is all this program is ever interested in.
  */
-char *getline(filep)
+char *get_line(filep)
 	register struct filepointer	*filep;
 {
 	register char	*p,	/* walking pointer */
diff -ur ../before/omni/src/tool/omkdepend/parse.c omni/src/tool/omkdepend/parse.c
--- ../before/omni/src/tool/omkdepend/parse.c	2008-12-29 15:38:31.000000000 +0100
+++ omni/src/tool/omkdepend/parse.c	2009-05-28 11:31:24.000000000 +0200
@@ -45,7 +45,7 @@
 	register int	type;
 	boolean recfailOK;
 
-	while (line = getline(filep)) {
+	while (line = get_line(filep)) {
 		switch(type = deftype(line, filep, file_red, file, TRUE)) {
 		case IF:
 		doif:
@@ -170,7 +170,7 @@
 	register char	*line;
 	register int	type;
 
-	while (line = getline(filep)) {
+	while (line = get_line(filep)) {
 		switch(type = deftype(line, filep, file_red, file, FALSE)) {
 		case IF:
 		case IFFALSE:

